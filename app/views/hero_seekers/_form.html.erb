<%= form_with(model: hero_seeker, local: true) do |form| %>
  <% if hero_seeker.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hero_seeker.errors.count, "error") %> prohibited this hero_seeker from being saved:</h2>

      <ul>
        <% hero_seeker.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :hero_id %>
    <%= form.number_field :hero_id %>
  </div>

  <div class="field">
    <%= form.label :seeker_id %>
    <%= form.number_field :seeker_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
